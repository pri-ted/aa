apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: service-etl-pdb
  namespace: platform-apps
  labels:
    app: service-etl
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: service-etl
