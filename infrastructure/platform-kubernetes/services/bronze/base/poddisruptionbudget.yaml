apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: service-bronze-pdb
  namespace: platform-apps
  labels:
    app: service-bronze
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: service-bronze
