apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: service-rules-pdb
  namespace: platform-apps
  labels:
    app: service-rules
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: service-rules
