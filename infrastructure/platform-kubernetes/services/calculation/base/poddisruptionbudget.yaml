apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: service-calculation-pdb
  namespace: platform-apps
  labels:
    app: service-calculation
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: service-calculation
