apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: service-gold-pdb
  namespace: platform-apps
  labels:
    app: service-gold
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: service-gold
